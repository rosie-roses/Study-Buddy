import React from "react";
import { View, Text} from "react-native";

const AddCoursesPageScreen = (props) => {
    return(
        <View><Text>We are in the add courses page.</Text></View>
    );
}

export default AddCoursesPageScreen;