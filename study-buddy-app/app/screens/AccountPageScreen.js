import React from "react";
import { View, Text} from "react-native";

const AccountPageScreen = (props) => {
    return(
        <View><Text>We are in the account page.</Text></View>
    );
}

export default AccountPageScreen;